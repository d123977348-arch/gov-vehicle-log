'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import {supabaseBrowser} from '@/lib/supabase-browser'
export default function Login(){const [email,setEmail]=useState('');const [pw,setPw]=useState('');const [err,setErr]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();setErr('');const {error}=await supabaseBrowser().auth.signInWithPassword({email,password:pw});if(error)setErr(error.message);else r.push('/dashboard')}return <main className="container" style={{maxWidth:520,paddingTop:80}}><div className="card"><h1>公務車使用紀錄管理系統</h1><p className="muted">多人線上版</p><form onSubmit={go}><div className="field"><label>帳號 Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>密碼</label><input value={pw} onChange={e=>setPw(e.target.value)} type="password" required/></div>{err&&<div className="notice">{err}</div>}<button className="btn primary" style={{width:'100%'}}>登入</button></form><p className="muted" style={{marginTop:16}}>管理員可在 Supabase Authentication 建立帳號。</p></div></main>}
