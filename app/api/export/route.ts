import {NextRequest,NextResponse} from 'next/server';import * as XLSX from 'xlsx';
export async function GET(req:NextRequest){const base=new URL(req.url);base.pathname='/api/records';const res=await fetch(base.toString());const {rows=[]}=await res.json();const wb=XLSX.utils.book_new();const by=new Map<string,any[]>();for(const r of rows){if(!by.has(r.plate))by.set(r.plate,[]);by.get(r.plate)!.push(r)}for(const [plate,items] of by){const data=items.map((x:any)=>({'使用日期':x.usage_date,'使用人':x.users,'起訖地點 (需填區域)':x.route,'出發時間':x.start_time,'結束時間':x.end_time,'出發里程':x.start_mileage,'結束里程':x.end_mileage,'用途':x.purposes,'加油時里程':x.fuel_mileages,'加油費':x.fuel_amount||'','備註':x.remark||''}));const ws=XLSX.utils.json_to_sheet(data);XLSX.utils.book_append_sheet(wb,ws,plate.slice(0,31))}const buf=XLSX.write(wb,{type:'buffer',bookType:'xlsx'});return new NextResponse(buf,{headers:{'Content-Type':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','Content-Disposition':'attachment; filename="vehicle-log.xlsx"'}})}
